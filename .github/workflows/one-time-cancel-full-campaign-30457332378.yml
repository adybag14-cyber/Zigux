name: Stop full campaign 30457332378

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/one-time-cancel-full-campaign-30457332378.yml

permissions:
  contents: read

concurrency:
  group: zigux-full-linux-source-coverage
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - run: echo "Run 30457332378 was superseded after the Rust apt conflict was confirmed."
