name: Stop defective full Linux campaign 30451881719

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/one-time-cancel-full-campaign-30451881719.yml

permissions:
  contents: read

concurrency:
  group: zigux-full-linux-source-coverage
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Confirm cancellation handoff
        run: echo "Run 30451881719 was superseded through the shared concurrency group."
