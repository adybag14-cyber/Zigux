# Phase 11 HVC Console Survey

This survey note now serves as the archival checkpoint for the original Phase 11 roadmap gap around `drivers/tty/hvc/hvc_console.c`.

The live archival packet now belongs to lane `P11-L16`.

The archived lane checkpoint is now:

- reviewed against archival `master` checkpoint `ee124761ef3ef5fcc6bb9cd8b7fe8d1fce326839`
- lane `P11-L16` keeps `drivers/tty/hvc/hvc_console.zig`, `drivers/tty/hvc/hvc_console_verify.zig`, and `zigux/tests/phase11_hvc_console.zig` aligned around the bounded starter, compile-local teardown truthfulness, setup-state slot validation, CRLF write framing, flush intent, teardown gating, final-close wait summaries, a tiny remove-path handoff summary, a tiny tty-registration handoff summary, a tiny sysrq handoff summary, a tiny notifier-facing handoff summary, and the compile-local notifierless-open, targetless-sysrq, no-dispatch sysrq notifier-deferral, never-registered notifier, targetless notifier, notifier-prerequisite failure-mode replays, cleanup-prerequisite failure-mode replays, khvcd worker-entry sleep, kick, and poll-mask boundary, and impossible hangup buffered-write failure-mode replays
- `zigux/tests/phase11_hvc_console_survey.zig` now keeps a bounded driver-local `struct winsize` layout checkpoint for the resize boundary, asserting size `8`, alignment `2`, and offsets `0`, `2`, `4`, and `6`, and a bounded `struct hv_ops` callback-table layout checkpoint for size `72`, alignment `8`, and callback-pointer offsets `0` through `64`, without widening into tty-core or `struct hvc_struct` ownership
- `Documentation/zigux/phase11-hvc-console-slice.md` records the active starter scope, `Documentation/zigux/phase11-hvc-console-validation-matrix.md` names the current shared gate plus the compile-local verifier, `zigux/tests/phase11_hvc_console_manifest.json` keeps the archival landing checkpoint explicit, `scripts/zigux/check-phase11-hvc-survey-packet.py` keeps the dedicated archival survey note, manifest, validation matrix, shared build step, `make -C zigux phase11-hvc-survey` route, and bootstrap workflow route fail-closed, `zigux/tests/phase11_hvc_cleanup.zig` keeps the bounded `hvc_cleanup()` tty-port release handoff, hangup-driven cleanup branch, and missing-reference cleanup failure-mode replay explicit beside that same shared packet, the landed tty-registration, sysrq, notifier-facing, notifierless-open, targetless-sysrq, no-dispatch sysrq notifier-deferral, never-registered notifier, targetless notifier, notifier-prerequisite, cleanup-prerequisite, khvcd worker-entry sleep, kick, and poll-mask boundary, and impossible hangup buffered-write failure-mode replays, `Documentation/zigux/phase11-hvc-console-teardown-note.md` keeps the close, cleanup, remove, write-to-hangup, and hangup-disconnect ownership split explicit in one driver-local note, including retry-after-`-EAGAIN`, fatal-drop without fabricated buffered bytes, partial-write carryover, stale-hangup buffered-byte preservation, resize-work cancellation, stale-count short-circuiting, notifier-hangup ownership, buffered-write clearing, and kept console binding, and `Documentation/zigux/README.md`, `scripts/zigux/README.md`, `Documentation/zigux/phase11-closure-note.md`, `Documentation/zigux/phase11-shared-replay-contract.md`, `Documentation/zigux/phase11-uapi-header-parity-survey.md`, `Documentation/zigux/review-checklist.md`, `scripts/zigux/check-phase11-shared-replay-contract.py`, `scripts/zigux/check-phase11-header-boundary-packet.py`, `zigux/tests/README.md`, `zigux/Makefile`, and `.github/workflows/zigux-bootstrap.yml` now keep those HVC review surfaces coupled to the wider Phase 11 replay route while preserving the focused shared header-boundary packet beside the broader HVC note
- against the Phase 11 roadmap, the current packet closed the straightforward-lifecycle simple-driver gap in bounded form by landing a reviewable direct-port driver starter in `drivers/tty/hvc/hvc_console.zig`, had its first hardware validation matrix in `Documentation/zigux/phase11-hvc-console-validation-matrix.md`, and kept teardown and failure-mode parity intentionally host-free across the close, cleanup, remove, tty-registration, sysrq, notifier, and notifier-state failure checkpoints rather than live tty or hypervisor integration
- repo reality now carries one bounded starter for each Phase 11 simple-production-driver roadmap anchor: `drivers/watchdog/gpio_wdt.zig`, `drivers/watchdog/bcm2835_wdt.zig`, `drivers/watchdog/dw_wdt.zig`, and `drivers/tty/hvc/hvc_console.zig`, so the remaining Phase 11 gap is live integration depth rather than missing starter coverage
- current `master` now also carries the bounded `P11-L14` khvcd polling-contract follow-through in `drivers/tty/hvc/hvc_console.zig` and `zigux/tests/phase11_hvc_console.zig`, while `drivers/tty/hvc/hvc_console_verify.zig` keeps the compile-local khvcd worker-entry sleep, kick, and poll-mask boundary replays explicit beside that same packet, making notifier-driven versus polling-driven wakeups, bounded reschedule intent, final-close wait carryover, and teardown-facing host-I/O pressure reviewable without claiming khvcd worker execution
- current `master` now also keeps the bounded `hvc_hangup()` disconnect boundary explicit as a stale hangup short-circuit that preserves buffered-write state when the port count is already zero, without widening into live notifier callbacks, remove-time tty ownership races, irq teardown, or host-backed disconnect timing
- current `master` may move independently after this checkpoint; the live truthfulness source for the lane is the current driver, verifier, tests, archival manifest, validation matrix, and shared replay contract rather than this archived commit pin alone
- this survey note remains as the checkpoint for the original gap that the bounded starter closed
- the remaining unported work is now live tty-driver registration, notifier callback execution, khvcd worker execution, live sysrq integration, and host-backed teardown validation

This lane is no longer survey-only, but the archival survey still does not claim live tty-driver registration, notifier callback execution, hvc polling kthread execution, live sysrq handling, early-console registration, or live hypervisor I/O.

The next honest bounded step inside the same Phase 11 lane is now another small shared-review truthfulness sync or similarly small watchdog or HVC teardown and failure-mode parity repair that stays inside the shipped `scripts/zigux/check-phase11-hvc-survey-packet.py` delivery packet, the shared `scripts/zigux/check-phase11-shared-replay-contract.py` contract, the focused `scripts/zigux/check-phase11-header-boundary-packet.py` route, and the shared-versus-dedicated replay packet, instead of reopening the already-landed shared-review checker hardening or widening into live callback execution or host-backed I/O.
