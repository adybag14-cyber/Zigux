# Phase 10 Virtio Driver Lane Sequencing

This note keeps the active Phase 10 virtio lab bundle split into bounded lanes so shared reminders do not collapse core, ring, input, and MMIO work into one noisy packet.

## Scope

Use this note when a Phase 10 change touches the shipped virtio closure packet recorded in `zigux/tests/phase10_closure_manifest.json`.

Keep the current lane split explicit:
- core lane `P10-L01` currently owns the direct core packet in `drivers/virtio/virtio.zig`, `drivers/virtio/virtio_driver_id.zig`, `drivers/virtio/virtio_verify.zig`, `zigux/tests/phase10_virtio_core.zig`, `zigux/tests/phase10_virtio_core_interrupt_compound_ack.zig`, `zigux/tests/phase10_virtio_core_reset_queue.zig`, `zigux/tests/phase10_virtio_driver_id.zig`, `zigux/tests/phase10_virtio_core_manifest.json`, `Documentation/zigux/phase10-virtio-core-slice.md`, `zigux/tests/phase10_virtio_core_survey.zig`, `Documentation/zigux/phase10-virtio-core-survey.md`, and `scripts/zigux/check-phase10-core-packet.py`; current `master` keeps those core helper, driver-id, verify, attribute-summary replay, compound-ack replay, reset replay, manifest, slice-note, survey-note, survey-gate, and checker surfaces reviewable together while `Documentation/zigux/phase10-virtio-core-slice.md` now stands as landed packet-local review evidence recorded by the dedicated core survey, so the next same-lane follow-through should stay inside one bounded core-only survey-gate, note, manifest, checker, replay, or ownership reminder repair rather than flattening the lane back to a driver-only story
- ring lane `P10-L10` currently tracks the documentation-first ring packet in `zigux/tests/phase10_virtio_ring_manifest.json`, `Documentation/zigux/phase10-virtio-ring-survey.md`, `Documentation/zigux/phase10-virtio-ring-slice.md`, `Documentation/zigux/phase10-virtio-driver-lane-sequencing.md`, `Documentation/zigux/phase10-closure-evidence.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/freeze-map.md`, and `scripts/zigux/README.md`; current `master` keeps those manifest, survey-note, slice-note, shared owner-map, closure-note, review-companion, freeze-boundary, and scripts-root reminder surfaces reviewable together while the direct ring helper, verify, replay, survey-gate, checker, and shared build-route files remain repo-reality gaps, so the next same-lane follow-through should stay inside one ring-only manifest, survey, slice-note, or owner-map truthfulness repair, or the first direct packet restoration step, instead of widening ring work back into MMIO-owned transport or broad shared reminder churn
- MMIO lane `P10-L11` currently owns the direct MMIO packet in `Documentation/zigux/phase10-virtio-mmio-survey.md`, `drivers/virtio/virtio_mmio.zig`, `drivers/virtio/virtio_mmio_verify.zig`, `zigux/tests/phase10_virtio_mmio.zig`, `zigux/tests/phase10_virtio_mmio_survey.zig`, `zigux/tests/phase10_virtio_mmio_manifest.json`, `scripts/zigux/check-phase10-mmio-packet.py`, and `scripts/zigux/check-phase10-mmio-freeze-boundary.py`; current `master` keeps those MMIO wrapper, focused verifier, replay, survey-gate, manifest, survey-note, and freeze-boundary surfaces reviewable together with the blocked risky-transport posture, so the next same-lane follow-through should stay inside one MMIO-only survey, manifest, checker, replay, or freeze-boundary truthfulness repair rather than reopening ring ownership or broad shared reminders
- input lane `P10-L22` currently owns the direct input packet in `Documentation/zigux/phase10-virtio-input-survey.md`, `drivers/virtio/virtio_input.zig`, `drivers/virtio/virtio_input_probe_preflight.zig`, `drivers/virtio/virtio_input_verify.zig`, `scripts/zigux/check-phase10-input-packet.py`, `zigux/tests/phase10_virtio_input.zig`, `zigux/tests/phase10_virtio_input_probe_preflight.zig`, `zigux/tests/phase10_virtio_input_queue_callback_preflight.zig`, `zigux/tests/phase10_virtio_input_registration_preflight.zig`, `zigux/tests/phase10_virtio_input_status_drain.zig`, `zigux/tests/phase10_virtio_input_teardown_observation.zig`, `zigux/tests/phase10_virtio_input_survey.zig`, `Documentation/zigux/phase10-virtio-input-slice.md`, `Documentation/zigux/phase10-virtio-input-module-slice.md`, and `zigux/tests/phase10_virtio_input_manifest.json`; public-tree rereads keep those direct input helper, probe-preflight, verify, queue-callback-preflight, registration-preflight, status-drain, teardown-observation, manifest, survey-note, survey-gate, slice-note, and module-slice surfaces reviewable together on current `master`, while `zigux/tests/phase10_virtio_input.zig` stays a counted packet reference rather than the directly re-readable replay this lane now leans on, and the adjacent shared build-graph follow-through for the probe-preflight, registration-preflight, and teardown-observation compile entries stays parked in `P10-L15`, so the next same-lane follow-through should stay inside one bounded validator, manifest, survey, checker, or owner-map truthfulness repair rather than regressing the packet back to a restore-only or reminder-only story
- shared docs-root plus harness-marker sync lane `P10-L04` now owns only future shared Phase 10 reminder rereads after the earlier `drivers/virtio/virtio_input_probe_preflight.zig` docs-root undercount, the docs-root ring exact-path reminder repair, and the scripts-root input-probe-preflight reminder repair all closed on current `master`. If that shared packet reopens again, start this lane with `scripts/zigux/README.md`, `Documentation/zigux/review-checklist.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/phase10-closure-evidence.md`, and `scripts/zigux/check-phase10-harness-coverage.py`, then narrow the follow-through to the next single shared reminder, checker, or manifest mismatch that a fresh reread actually proves before widening into `Documentation/zigux/README.md`, `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`, broader closure-note prose, route wiring, or input-lane helper delivery

The closure manifest remains the commit-pinned inventory for the broader packet, including the packet-local slice and survey note paths, the blocked risky-transport posture, the allowed destination families, and the Phase 5, Phase 9, and Phase 14 boundary evidence.

## Owner Split

Keep the current owner map explicit:
- core truthfulness owns the direct core reminder surfaces in `Documentation/zigux/README.md`, `Documentation/zigux/review-checklist.md`, `zigux/tests/README.md`, and `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`
- core packet review currently owns the direct core packet across `drivers/virtio/virtio.zig`, `drivers/virtio/virtio_driver_id.zig`, `drivers/virtio/virtio_verify.zig`, `zigux/tests/phase10_virtio_core.zig`, `zigux/tests/phase10_virtio_core_interrupt_compound_ack.zig`, `zigux/tests/phase10_virtio_core_reset_queue.zig`, `zigux/tests/phase10_virtio_driver_id.zig`, `zigux/tests/phase10_virtio_core_manifest.json`, `Documentation/zigux/phase10-virtio-core-slice.md`, `zigux/tests/phase10_virtio_core_survey.zig`, `Documentation/zigux/phase10-virtio-core-survey.md`, and `scripts/zigux/check-phase10-core-packet.py`; shared reminders should keep those direct core helper, driver-id, verify, attribute-summary replay, compound-ack replay, reset replay, manifest, slice-note, survey-note, survey-gate, and checker surfaces explicit while the remaining same-lane follow-through stays inside the dedicated core survey gate rather than drifting back into vague shared wording
- ring packet review currently owns the documentation-first ring packet across `zigux/tests/phase10_virtio_ring_manifest.json`, `Documentation/zigux/phase10-virtio-ring-survey.md`, `Documentation/zigux/phase10-virtio-ring-slice.md`, `Documentation/zigux/phase10-virtio-driver-lane-sequencing.md`, `Documentation/zigux/phase10-closure-evidence.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/freeze-map.md`, and `scripts/zigux/README.md`; shared reminders should keep those manifest, survey-note, slice-note, shared owner-map, closure-note, review-companion, freeze-boundary, and scripts-root reminder surfaces explicit while the direct ring helper, verify, replay, survey-gate, checker, and shared build-route files stay explicit repo-reality gaps until a ring-only restoration lane lands them again
- input packet review currently owns the direct input packet across `Documentation/zigux/phase10-virtio-input-survey.md`, `drivers/virtio/virtio_input.zig`, `drivers/virtio/virtio_input_probe_preflight.zig`, `drivers/virtio/virtio_input_verify.zig`, `scripts/zigux/check-phase10-input-packet.py`, `zigux/tests/phase10_virtio_input.zig`, `zigux/tests/phase10_virtio_input_probe_preflight.zig`, `zigux/tests/phase10_virtio_input_queue_callback_preflight.zig`, `zigux/tests/phase10_virtio_input_registration_preflight.zig`, `zigux/tests/phase10_virtio_input_status_drain.zig`, `zigux/tests/phase10_virtio_input_teardown_observation.zig`, `zigux/tests/phase10_virtio_input_survey.zig`, `Documentation/zigux/phase10-virtio-input-slice.md`, `Documentation/zigux/phase10-virtio-input-module-slice.md`, and `zigux/tests/phase10_virtio_input_manifest.json`; shared reminders should keep those direct helper, probe-preflight, verify, queue-callback-preflight, registration-preflight, status-drain, teardown-observation, checker, manifest, survey-note, survey-gate, slice-note, and module-slice surfaces explicit, while `zigux/tests/phase10_virtio_input.zig` stays a counted packet reference rather than the directly re-readable replay path and the adjacent compile-path follow-through for the probe-preflight, registration-preflight, and teardown-observation build entries stays parked in `P10-L15`, instead of collapsing the lane back to a manifest-only reminder
- `P10-L11` MMIO lane owns MMIO helper footing, the risky-transport freeze boundary, and MMIO-local transport posture evidence: `Documentation/zigux/phase10-virtio-mmio-survey.md`, `drivers/virtio/virtio_mmio.zig`, `drivers/virtio/virtio_mmio_verify.zig`, `zigux/tests/phase10_virtio_mmio.zig`, `zigux/tests/phase10_virtio_mmio_survey.zig`, `zigux/tests/phase10_virtio_mmio_manifest.json`, `scripts/zigux/check-phase10-mmio-packet.py`, and `scripts/zigux/check-phase10-mmio-freeze-boundary.py`; shared reminders should keep those MMIO wrapper, focused verifier, replay, survey-gate, manifest, survey-note, and freeze-boundary surfaces explicit while the next same-lane follow-through stays inside one MMIO-only survey, manifest, checker, replay, or freeze-boundary repair instead of treating MMIO lane identity as parked drift
- shared closure truthfulness owns `Documentation/zigux/phase10-closure-evidence.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `scripts/zigux/README.md`, `zigux/tests/README.md`, `zigux/tests/phase10_closure_manifest.json`, `zigux/tests/phase10_build.zig`, `zigux/Makefile`, `make -C zigux phase10-validate`, `zig build test --build-file zigux/tests/phase10_build.zig`, `make -C zigux phase10-test`, and `make -C zigux phase10`
- shared docs-root plus harness-marker sync under `P10-L04` now owns only future shared reminder rereads under `Documentation/zigux/review-checklist.md` plus the marker block in `scripts/zigux/check-phase10-harness-coverage.py` because the earlier docs-root `drivers/virtio/virtio_input_probe_preflight.zig` undercount, the docs-root ring exact-path reminder repair, and the scripts-root input-probe-preflight reminder repair are already closed on current `master`. If a future shared reminder reread still needs `P10-L04`, start with `scripts/zigux/README.md`, `Documentation/zigux/review-checklist.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/phase10-closure-evidence.md`, and `scripts/zigux/check-phase10-harness-coverage.py`, then narrow the follow-through to the next single shared reminder, checker, or manifest mismatch that a fresh reread actually proves before reopening `Documentation/zigux/README.md`, `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`, broader closure-note prose, route wiring, or input-lane helper delivery

## Shared Packet Surfaces

When a real Phase 10 change lands, keep these shared surfaces aligned:
- `Documentation/zigux/README.md`
- `Documentation/zigux/review-checklist.md`
- `Documentation/zigux/freeze-map.md`
- `Documentation/zigux/phase10-closure-evidence.md`
- `Documentation/zigux/phase10-virtio-core-slice.md`
- `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`
- `Documentation/zigux/phase10-virtio-ring-slice.md`
- `Documentation/zigux/phase10-virtio-input-slice.md`
- `Documentation/zigux/phase10-virtio-input-module-slice.md`
- `Documentation/zigux/phase10-virtio-mmio-slice.md`
- `scripts/zigux/README.md`
- `zigux/tests/README.md`
- `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`
- `scripts/zigux/check-phase10-harness-coverage.py`
- `zigux/tests/phase10_closure_manifest.json`
- `zigux/tests/phase10_build.zig`
- `zigux/Makefile`
- `make -C zigux phase10-validate`
- `zig build test --build-file zigux/tests/phase10_build.zig`
- `make -C zigux phase10-test`
- `make -C zigux phase10`

## Sequencing Rules

Use this note to keep the bounded work order honest:
1. Prefer one virtio lane at a time instead of batching core, ring, input, and MMIO reminders into one mixed change.
2. Keep the current validator posture explicit: the shared scripts-root Phase 10 truthfulness packet on current `master` is `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`, `scripts/zigux/check-phase10-harness-coverage.py`, `scripts/zigux/validate-phase10.py`, `scripts/zigux/validate-phase10-closure.py`, and the build-backed replay routes, so reminder-surface edits should stay aligned with those shipped checks instead of implying a broader dedicated per-lane checker stack.
3. Keep risky transport blocked: do not imply queue setup or reset parity, IRQ parity, DMA paths, input registration lifecycle parity, or probe or remove lifecycle closure beyond the blocked gaps already recorded in `zigux/tests/phase10_closure_manifest.json`.
4. Treat the separate Phase 5 `reference_samples` boundary and the separate Phase 9 `runtime_starters` boundary as adjacent evidence only, not counted Phase 10 closure progress.
5. Keep the separate Phase 14 study-only ownership of `kernel/workqueue.c` and `kernel/trace/ring_buffer.c` explicit instead of letting those anchors drift into the Phase 10 virtio packet.
6. When a shared reminder surface refreshes freeze-boundary wording, keep the parked `P10-L11` owner, the directly readable `drivers/virtio/virtio_mmio_verify.zig` verifier, and the rollback-owner note explicit around `Documentation/zigux/freeze-map.md`, `Documentation/zigux/phase10-virtio-mmio-survey.md`, and `scripts/zigux/check-phase10-mmio-freeze-boundary.py` instead of letting that policy drift back into anonymous shared wording.
7. The earlier docs-root omission of `drivers/virtio/virtio_input_probe_preflight.zig`, the docs-root ring exact-path reminder repair, and the scripts-root input-probe-preflight reminder repair are already closed on current `master`; the next preset shared reminder reopen for `P10-L04` is therefore no longer a specific queued file fix. If a future shared reminder reread still needs `P10-L04`, start with `scripts/zigux/README.md`, `Documentation/zigux/review-checklist.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/phase10-closure-evidence.md`, and `scripts/zigux/check-phase10-harness-coverage.py`, then keep the follow-through limited to the next single shared reminder, checker, or manifest mismatch that a fresh reread actually proves before reopening `Documentation/zigux/README.md`, `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`, broader closure-note prose, route wiring, or input-lane helper delivery.
8. When a broad reminder surface changes, prefer the directly shipped closure-manifest plus the current ring and input packet boundaries over speculative packet-local inventory growth. Keep the ring lane on `zigux/tests/phase10_virtio_ring_manifest.json`, `Documentation/zigux/phase10-virtio-ring-survey.md`, `Documentation/zigux/phase10-virtio-ring-slice.md`, `Documentation/zigux/phase10-virtio-driver-lane-sequencing.md`, `Documentation/zigux/phase10-closure-evidence.md`, `Documentation/zigux/phase10-phase11-phase13-tests-root-review-companion.md`, `Documentation/zigux/freeze-map.md`, and `scripts/zigux/README.md` while the direct ring helper, verify, replay, checker, survey-gate, and shared build-route files stay explicit repo-reality gaps, and keep the input lane on `Documentation/zigux/phase10-virtio-input-survey.md`, `drivers/virtio/virtio_input.zig`, `drivers/virtio/virtio_input_probe_preflight.zig`, `drivers/virtio/virtio_input_verify.zig`, `scripts/zigux/check-phase10-input-packet.py`, `zigux/tests/phase10_virtio_input.zig`, `zigux/tests/phase10_virtio_input_probe_preflight.zig`, `zigux/tests/phase10_virtio_input_queue_callback_preflight.zig`, `zigux/tests/phase10_virtio_input_registration_preflight.zig`, `zigux/tests/phase10_virtio_input_status_drain.zig`, `zigux/tests/phase10_virtio_input_teardown_observation.zig`, `zigux/tests/phase10_virtio_input_survey.zig`, `Documentation/zigux/phase10-virtio-input-slice.md`, `Documentation/zigux/phase10-virtio-input-module-slice.md`, and `zigux/tests/phase10_virtio_input_manifest.json` as the current landed packet, with `zigux/tests/phase10_virtio_input.zig` kept as counted packet vocabulary rather than the directly re-readable replay path, and keep the adjacent probe-preflight, registration-preflight, and teardown-observation compile-path follow-through parked in `P10-L15` instead of reopening the shared build graph from inside same-lane reminder work.
9. Keep the MMIO lane identity aligned with the live `P10-L11` survey, manifest, checker, and freeze-boundary packet; if a future reread finds actual MMIO-owner drift again, repair that one MMIO-only owner-map or checker surface instead of reusing MMIO packet labels to widen ring ownership, shared owner-map edits, or freeze-boundary reminder work.
10. The shipped focused shared Phase 10 scripts on current `master` are `scripts/zigux/check-phase10-tests-readme-core-surfaces.py`, `scripts/zigux/check-phase10-harness-coverage.py`, `scripts/zigux/check-phase10-input-packet.py`, `scripts/zigux/validate-phase10.py`, and `scripts/zigux/validate-phase10-closure.py`, so same-lane reminder work should stay anchored to the direct driver, docs, manifest, tests-root, validator, and shared-checker surfaces unless a new focused script actually lands.

## Non-Goals

This note does not widen Phase 10 into:
- direct risky-transport or lifecycle parity claims
- a dedicated shared validator stack beyond the shipped focused reminder checkers, validator routes, and build-backed replay routes
- Phase 5 sample closure, Phase 9 runtime-loader closure, or Phase 14 study-only evidence as counted virtio-driver progress
- a claim that the Phase 10 tranche is closed or ready for an Architecture Council reopen