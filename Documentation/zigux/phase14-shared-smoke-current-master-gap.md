# Phase 14 Shared Smoke Current-Master Gap

This note records one bounded Phase 14 productization-evidence gap on current `master`.

## Status

- `PHASE14_GAP_KIND=shared_smoke_current_master_readback_gap`
- `PHASE14_LANE_KEY=P14-L05`
- `PHASE14_PROVENANCE_MODE=dated_master_readback`
- re-read against fresh current-master recovery on 2026-05-18
- role: keep the shared Phase 14 evidence packet truthful when this lane can recover the documentation layer directly, recover `scripts/zigux/validate-phase14.py` again through pinned blob readback, and now recover `zigux/Makefile` again through the contents path while that live Makefile body no longer exposes the older Phase 14 rerun routes named by saved reminder notes

## Why this gap matters

The Phase 14 roadmap keeps core-adjacent work in a bounded study-only or stay-in-C posture. That means reviewability depends on current product evidence being directly readable and not just inherited from older shared reminder text.

Current `master` now exposes more of the shared smoke packet than this note previously recorded, but the packet is still only partially recoverable through the exact read modes available in this lane.

That makes the honest same-lane task narrower than reviving old packet claims: record the updated split between the directly readable documentation layer, the recovered validator body, the newly readable current Makefile body, and the still-missing executable layer so later shared-surface repairs start from repo reality instead of from stale reminder text.

## Current-Master Companions Recoverable In This Lane

Fresh current-master rereads on 2026-05-18 now recover these shared-smoke and governance companions across the lane's current readback mix:

- `Documentation/zigux/phase14-end-to-end-smoke-survey.md`
- `Documentation/zigux/phase14-core-boundary-traceability.md`
- `Documentation/zigux/phase14-release-boundary-survey.md`
- `Documentation/zigux/freeze-map.md`
- `Documentation/zigux/phase14-skbuff-bridge-survey.md`
- `Documentation/zigux/phase15-study-only-anchor-accounting.md`
- `Documentation/zigux/README.md`
- `Documentation/zigux/review-checklist.md`
- `zigux/tests/README.md`
- `zigux/Makefile`
- `scripts/zigux/validate-phase14.py`

Those readable notes and paths keep the current narrow posture visible:

- the shared smoke note, cross-anchor traceability note, freeze map, docs-root summary, checklist, and tests-root reminder are directly readable again through this lane's contents path
- `Documentation/zigux/phase14-release-boundary-survey.md` is still recoverable through mixed repo readback, which is enough to compare its wording against the now-readable current `zigux/Makefile` body
- `scripts/zigux/validate-phase14.py` is recoverable again through pinned blob readback and now carries a real shared-smoke validator surface instead of the older placeholder body
- `zigux/Makefile` is directly readable again through the contents path, but its live body currently exposes only the Phase 2 toolchain and kbuild routes and no `phase14-validate`, `phase14-smoke`, `phase14-test`, or `phase14` targets
- that means the older validator-local exact-line handoff is no longer the next honest same-lane fix: before revisiting `MAKE_EXACT_LINES`, the broader shared smoke notes need to stop claiming that the current Makefile already reruns `scripts/zigux/check-phase14-tests-readme-smoke-summary.py` inside `phase14-validate`
- `zigux/tests/README.md` now already carries the broadened study-only Phase 14 packet and the Phase-2-only Makefile warning, so the older tests-root undercount named by this note is now closed on current `master`
- the live skbuff packet is still parked on `phase14-skbuff-live-ownership-blocker`
- the skbuff note still records seven review-only boundary-map areas and `full_bundle_only` compile evidence
- the Phase 15 companion still keeps `kernel/workqueue.c` and `kernel/trace/ring_buffer.c` explicit as study-only anchors rather than active port claims

## Remaining Shared-Smoke Readback Gap

On the same current `master`, the GitHub contents read path used in this lane still returns `404` for the executable and machine-readable shared-smoke packet members below:

- `scripts/zigux/check-phase14-release-boundary-exact-counts.py`
- `zigux/tests/phase14_build.zig`
- `zigux/tests/phase14_end_to_end_smoke_manifest.json`
- `zigux/tests/phase14_end_to_end_smoke_survey.zig`
- `zigux/tests/phase14_workqueue_bridge.zig`
- `zigux/tests/phase14_skbuff_bridge.zig`
- `zigux/tests/phase14_ring_buffer_survey.zig`
- `zigux/tests/phase14_rcu_tree_survey.zig`
- `net/core/skbuff_bridge.zig`

This note does not claim those paths are globally absent from every repo access mode. It records the narrower fact that this lane's exact current-master contents readback still misses them, while `scripts/zigux/validate-phase14.py` is presently recoverable only through pinned blob readback rather than the same path-based contents bridge. Broad reminder text should not present that executable layer as directly re-read current-master evidence in this environment without a fresh successful readback of the missing files.

## Current Bounded Conclusion

The directly recoverable Phase 14 evidence on current `master` is broader than this note said on 2026-05-17, but it has also shifted in a way that makes two older same-lane follow-throughs stale.

It is no longer truthful to describe `scripts/zigux/validate-phase14.py` as a placeholder-only return. It is also no longer truthful to treat `zigux/Makefile` as live current-master evidence for the older `phase14-*` rerun routes, because the current readable Makefile body has collapsed back to a Phase 2-only surface.

The current productization gap is therefore the mismatch between the recovered shared-smoke documentation packet, the blob-readable validator body, the now-readable Phase 2-only Makefile body, the already-aligned tests-root reminder, and the still-missing executable layer beneath them.

That updated split closes the older tests-root undercount as the next honest same-lane fix. `zigux/tests/README.md` now already names `Documentation/zigux/phase14-core-boundary-traceability.md`, `Documentation/zigux/phase14-release-boundary-survey.md`, `Documentation/zigux/freeze-map.md`, and `Documentation/zigux/phase14-skbuff-bridge-survey.md` beside the broader shared smoke packet and the readable Phase-2-only Makefile warning.

The smallest remaining reminder drift inside this lane is now narrower: the mixed-readback `Documentation/zigux/phase14-release-boundary-survey.md` still needs to be refreshed so it stops treating `make -C zigux phase14-validate`, `make -C zigux phase14-smoke`, `make -C zigux phase14-test`, and `make -C zigux phase14` as current wrapper-backed proof while the readable current `zigux/Makefile` body remains Phase-2-only.

## Next Bounded Step

If a later run regains a safe same-lane write path for the broader shared reminder surfaces, narrow `Documentation/zigux/phase14-release-boundary-survey.md` first so it keeps the recovered study-only packet explicit, keeps the blob-readable validator posture visible, and moves the older `phase14-*` route names into packet-local or repo-reality-gap wording until a fresh reread proves those wrappers have returned on current `master`.

If a later run also restores direct current-master evidence for `phase14-validate`, `phase14-smoke`, or the dedicated Phase 14 build and manifest files, re-evaluate whether the parked one-file validator hardening should be revived or replaced before widening elsewhere.

If a later run restores exact readback for the missing executable packet members without restoring the old Makefile routes, keep the shared smoke packet aligned with that narrower repo reality instead of reviving stale validator-first wording.
