# Phase 14 Workqueue Bridge Slice

This bounded Phase 14 slice starts `kernel/workqueue_bridge.zig` as a pure boundary map anchored to `kernel/workqueue.c`.

The current bridge stays intentionally narrow:
- records the caller-facing submission boundary around `queue_work_on()` and `__queue_work()` without claiming live enqueue, wakeup, or execution behavior
- records the allocation and attribute boundary around `__alloc_workqueue()` and `devm_alloc_workqueue()` as future wrapper candidates only
- records the runtime `max_active` and ordered-workqueue retuning boundary for review, but keeps `workqueue_set_max_active()` and `pwq_adjust_max_active()` as explicit stay-in-C governance points because rebalance decisions still move queued items between `inactive_works` and execution-eligible state under the shipped C locking model
- records the flush and cancel boundary for review, but keeps `__flush_workqueue()` and `drain_workqueue()` as explicit stay-in-C governance points because active-color progression, chained flusher ownership, and `pwq->nr_in_flight` coordination still belong to the shipped C implementation
- marks `manage_workers()` and the `worker_pool` state machine as explicit stay-in-C decisions
- marks hotplug-driven worker migration and topology rebinding through `POOL_DISASSOCIATED`, `wq_online_cpumask`, `wq_unbound_cpumask`, and `unbound_wq_update_pwq()` as explicit stay-in-C decisions instead of a wrapper claim
- marks `rescuer_thread()`, `wq_worker_running()`, and `wq_worker_sleeping()` as explicit stay-in-C decisions tied to scheduler-visible worker-state ownership, `WORKER_NOT_RUNNING`, `pool->nr_running`, idle wakeups, and rescuer or mayday recovery
- adds a fifteen-checkpoint concurrency audit outline that names manager-role serialization, `worker_pool` forward-progress fields, the `__queue_work()` `max_active` or ordered inactive-list gate, the separate runtime `workqueue_set_max_active()` or `pwq_adjust_max_active()` reconfiguration window, the `try_to_grab_pending()` plus `queue_work_on()` pending-bit claim handoff, the unbound `__queue_work()` `pwq->refcnt` retry contract, the delayed-submission alias fan-in through `queue_delayed_work_on()`, `mod_delayed_work_on()`, and `__queue_delayed_work()`, the `delayed_work_timer_fn()` timer-expiry handoff back into `__queue_work()`, the explicit `mod_delayed_work_on()` requeue governance point, the `__flush_workqueue()` plus `drain_workqueue()` active-color ownership seam, the scheduler-visible worker-state transitions around `WORKER_NOT_RUNNING`, `pool->nr_running`, and idle wakeups, the `__queue_work()` `last_pool->lock` handoff, the `process_one_work()` unlock or relock execution window, the `worker_thread()` idle sleep transition, and the rescuer mayday handoff while keeping all live ownership in C

This slice still does not claim live worker pools, work execution, hotplug transitions, runtime `max_active` retuning ownership, flush semantics, cancellation completion, mayday escalation, rescuer threads, or scheduler-visible worker-state transitions.

The current lane now also records the delayed timer-expiry handoff through `delayed_work_timer_fn()` and back into `__queue_work()`, the explicit stay-in-C decision around `mod_delayed_work_on()` requeue ownership, the explicit runtime `max_active` retuning boundary around `workqueue_set_max_active()` and `pwq_adjust_max_active()`, the explicit flush-drain stay-in-C decision around `__flush_workqueue()` and `drain_workqueue()`, the hotplug-topology rebinding boundary around `POOL_DISASSOCIATED`, `wq_online_cpumask`, `wq_unbound_cpumask`, and `unbound_wq_update_pwq()`, and the explicit scheduler-visible worker-state note around `WORKER_NOT_RUNNING` plus `pool->nr_running`, while keeping timer-base, CPU-affinity, delayed-work rearm rules, ordered-workqueue rebalancing ownership, active-color progression, flush completion, worker migration, and rescuer execution in C.

The next honest bounded step is to leave this packet in blocked maintenance unless a future run can tighten one equally small rescuer or mayday stay-in-C note without claiming live delayed-work execution, hotplug rebinding ownership, runtime `max_active` retuning ownership, or flush completion.
