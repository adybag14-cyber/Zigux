{
  "lane_key": "P5-L22",
  "phase": "Phase 5",
  "surveyed_commit": "4387decfa0a96506f8207b81f74c8ac794a006a6",
  "anchor": "samples/kprobes/kretprobe_example.c",
  "sample_path": "samples/zigux/kretprobe_example.zig",
  "validation_entrypoint": "zig test samples/zigux/kretprobe_example.zig && zig test --dep kretprobe_example_sample -Mroot=zigux/tests/phase5_kretprobe_example.zig -Mkretprobe_example_sample=samples/zigux/kretprobe_example.zig && zig test zigux/tests/phase5_kretprobe_example_survey.zig",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest and survey test still describe the exact skip, return-value, duration, and missed-summary contract run through the focused kretprobe sample checks",
    "the sample keeps the Linux my_data-style private entry timestamp explicit and reviewable instead of burying it in unstructured state",
    "symbol retargeting stays a pre-init in-memory choice instead of implying module_param or runtime registration parity",
    "maxactive stays a pre-init in-memory choice and replayed summary field instead of implying concurrent kretprobe pressure parity",
    "sample-behavior changes update the manifest-backed replay, ownershipSummary()/runOwnershipReplay() lifecycle packet, and teardown contract, and the shared reminder packet keeps scripts/zigux/check-phase5-review-guide-surface.py plus zigux/tests/phase5_build.zig explicit as the shipped guide-surface guard and current directly readable shared build-route companion evidence instead of leaving those contributor cues implied",
    "docs and tests still keep register_kretprobe, unregister_kretprobe, pt_regs return extraction, and loadable module wiring out of scope for this Phase 5 sample"
  ],
  "exact_checks": [
    {
      "id": "default-symbol",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps kernel_clone as the default symbol name while allowing pre-init retargeting"
    },
    {
      "id": "retargeted-symbol",
      "kind": "anchor_shape",
      "expected": "pre-init retargetSymbol(\"do_sys_openat2\") survives init so the bounded sample keeps symbol retargeting explicit without claiming module_param parity"
    },
    {
      "id": "skip-kernel-thread",
      "kind": "bounded_handler_path",
      "expected": "runAnchorReplay checks that an entry with no current mm is skipped instead of arming a tracked instance"
    },
    {
      "id": "private-data-shape",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps a single entry-timestamp private-data record so the Linux my_data shape remains explicit as one i64-sized word"
    },
    {
      "id": "return-duration",
      "kind": "bounded_handler_path",
      "expected": "the replay records retval 42 and duration 75 ns after an entry timestamp of 100 and a return timestamp of 175"
    },
    {
      "id": "maxactive-retarget",
      "kind": "bounded_summary",
      "expected": "pre-init retargetMaxactive(3) survives init and replay so the bounded sample keeps maxactive explicit without claiming concurrent kretprobe pressure parity"
    },
    {
      "id": "missed-summary",
      "kind": "bounded_summary",
      "expected": "the replay records one missed instance so the exit-side nmissed summary remains reviewable without claiming register_kretprobe pressure parity"
    },
    {
      "id": "outstanding-instance-boundary",
      "kind": "ownership_lifetime",
      "expected": "ownershipSummary() and runOwnershipReplay() keep the armed sample explicit until retHandler clears the outstanding tracked instance"
    },
    {
      "id": "post-exit-rejection",
      "kind": "ownership_lifetime",
      "expected": "after exit the sample rejects later recordMissedInstance or handler calls while ownershipSummary() keeps the exited snapshot explicit"
    }
  ],
  "non_goals": [
    "register_kretprobe parity",
    "unregister_kretprobe parity",
    "pt_regs or regs_return_value parity",
    "loadable module wiring"
  ]
}
