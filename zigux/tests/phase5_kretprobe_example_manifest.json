{
  "lane_key": "P5-L18",
  "phase": "Phase 5",
  "surveyed_commit": "7361ac51374149a96b7a7a2c6ea3c995d8cc1231",
  "anchor": "samples/kprobes/kretprobe_example.c",
  "sample_path": "samples/zigux/kretprobe_example.zig",
  "validation_entrypoint": "zig build test --build-file zigux/tests/phase5_build.zig --summary all",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest and survey test still describe the exact sample-owned runRetargetReplay() symbol-selection guard, skip, return-value, duration, missed-summary, lifecycle-guard, runRecoveryReplay() teardown recovery, and fixed maxactiveBudget() = 20 cue run through phase5_build.zig, with runLifecycleGuardReplay() keeping pre-init and post-init guard boundaries explicit",
    "the sample keeps the Linux my_data-style private entry timestamp explicit and reviewable instead of burying it in unstructured state",
    "symbol retargeting stays a pre-init in-memory choice instead of implying module_param or runtime registration parity",
    "ownershipSummary keeps cold, initialized, armed, replay_complete, and exited snapshots explicit instead of leaving lifecycle review hidden in raw fields",
    "sample-owned runRetargetReplay() keeps empty-symbol rejection, pre-init retargeting, and post-init retarget rejection explicit, while runRecoveryReplay() keeps outstanding-instance rejection, timestamp-order rejection and recovery, and post-exit handler rejection explicit instead of leaving teardown hidden in external tests",
    "docs and tests still keep register_kretprobe, unregister_kretprobe, pt_regs return extraction, and runtime module wiring out of scope for this Phase 5 sample while keeping the separate Phase 9 runtime_kretprobe starter explicit"
  ],
  "exact_checks": [
    {
      "id": "default-symbol",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps kernel_clone as the default symbol name while allowing pre-init retargeting"
    },
    {
      "id": "retarget-replay",
      "kind": "anchor_shape",
      "expected": "runRetargetReplay keeps empty-symbol rejection, pre-init retargeting to do_sys_openat2, initialized post-retarget state, and post-init retarget rejection explicit"
    },
    {
      "id": "skip-kernel-thread",
      "kind": "bounded_handler_path",
      "expected": "runAnchorReplay checks that an entry with no current mm is skipped instead of arming a tracked instance"
    },
    {
      "id": "private-data-shape",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps a single entry-timestamp private-data record so the Linux my_data shape remains explicit as one i64-sized word"
    },
    {
      "id": "return-duration",
      "kind": "bounded_handler_path",
      "expected": "the replay records retval 42 and duration 75 ns after an entry timestamp of 100 and a return timestamp of 175"
    },
    {
      "id": "maxactive-budget",
      "kind": "bounded_handler_path",
      "expected": "the replay keeps maxactiveBudget() at 20 so the fixed review-only budget cue stays explicit without implying runtime registration-pressure handling"
    },
    {
      "id": "missed-summary",
      "kind": "bounded_summary",
      "expected": "the replay records one missed instance so the exit-side nmissed summary remains reviewable without claiming register_kretprobe pressure parity"
    },
    {
      "id": "ownership-summary-snapshots",
      "kind": "ownership_lifetime",
      "expected": "ownershipSummary keeps cold, initialized, armed, replay_complete, and exited snapshots explicit with active instance and entry-timestamp state"
    },
    {
      "id": "lifecycle-guard-replay",
      "kind": "ownership_lifetime",
      "expected": "runLifecycleGuardReplay keeps the pre-init anchor and exit rejections, double-init rejection, post-init retarget rejection, and initialized post-init state explicit without implying runtime registration parity"
    },
    {
      "id": "outstanding-instance-boundary",
      "kind": "ownership_lifetime",
      "expected": "runRecoveryReplay keeps direct retargeting, exit rejection while armed, invalid timestamp recovery, and the recovered duration 60 ns explicit until retHandler clears the outstanding tracked instance"
    },
    {
      "id": "post-exit-rejection",
      "kind": "ownership_lifetime",
      "expected": "after exit the sample rejects later recordMissedInstance or handler calls, with runRecoveryReplay keeping those post-exit boundaries explicit"
    }
  ],
  "non_goals": [
    "register_kretprobe parity",
    "unregister_kretprobe parity",
    "pt_regs or regs_return_value parity",
    "runtime module wiring"
  ]
}
