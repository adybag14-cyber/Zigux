{
  "lane_key": "P9-L16",
  "phase": "Phase 9",
  "surveyed_commit": "9c4fb93b29fd1b1c5dabb4cff3017a9ad5d9f1be",
  "anchor": "samples/kprobes/kretprobe_example.c",
  "roadmap_destinations": [
    "zigux/tests/runtime_*",
    "samples/zigux/runtime_*"
  ],
  "sample_path": "samples/zigux/runtime_kretprobe.zig",
  "validation_entrypoint": "zig build test --build-file zigux/tests/phase9_build.zig --summary all",
  "survey_summary": {
    "kretprobe_example_c_lines": 108,
    "preexisting_runtime_kretprobe_test_files": 3,
    "preexisting_runtime_kretprobe_sample_present": true,
    "preexisting_phase9_build_present": true,
    "preexisting_runtime_kretprobe_doc_present": true
  },
  "review_prompts": [
    "the review packet still keeps the Linux anchor, bounded lifecycle-summary surface, and the current released_without_substrate rollback path explicit instead of implying a real register or unregister handoff",
    "the manifest-backed delivery_evidence_catalog and ownership_map still keep the survey note, module-slice note, sample starter, survey gate, module gate, diff gate, loader scaffold, shared loader binding, shared loader-gap note, and shared Phase 9 build entrypoint in one reviewable packet",
    "the shared Phase 9 build entrypoint still names the dedicated phase9-runtime-kretprobe-sample-tests, phase9-runtime-kretprobe-module-tests, phase9-runtime-kretprobe-diff-tests, phase9-runtime-kretprobe-loader-tests, and phase9-runtime-kretprobe-survey-tests legs instead of leaving the shipped replay surface implicit",
    "sample or loader-behavior changes update the manifest-backed exact checks alongside samples/zigux/runtime_kretprobe.zig, runtime_kretprobe_module.zig, runtime_kretprobe_diff.zig, and the survey docs instead of leaving reviewers to infer the new contract from code alone",
    "the survey packet keeps the broader shared runtime-loader command-name, argv-policy, and environment-derived activation handling blocked so the kretprobe pilot remains pre-execution",
    "the survey packet still compares the landed selftest-hook starter against the remaining `first loadable Zigux runtime modules` and `runtime module lifecycle parity` roadmap steps instead of treating this bounded pilot module as Phase 9 closure"
  ],
  "exact_checks": [
    {
      "id": "lifecycle-summary-surface",
      "kind": "summary_contract",
      "expected": "the bounded starter exposes RuntimeKretprobeSummary with lifecycle stage, init or selftest or exit counters, active-instance state, and the latest bounded probe results so selftest and post-exit review never depend on internal field reads"
    },
    {
      "id": "loader-rollback-surface",
      "kind": "runtime_loader_contract",
      "expected": "the loader scaffold keeps register and unregister API names, explicit init and exit symbols, waiting_on_runtime_substrate handoff, and a released_without_substrate rollback path machine-checkable through the shared runtime-loader request surface"
    },
    {
      "id": "shared-build-leg-surface",
      "kind": "shared_build_contract",
      "expected": "the shared Phase 9 build keeps dedicated phase9-runtime-kretprobe-sample-tests, phase9-runtime-kretprobe-module-tests, phase9-runtime-kretprobe-diff-tests, phase9-runtime-kretprobe-loader-tests, and phase9-runtime-kretprobe-survey-tests legs explicit so the shipped replay packet stays first-class review evidence"
    },
    {
      "id": "delivery-ownership-packet",
      "kind": "review_surface",
      "expected": "the manifest-backed delivery catalog and ownership map keep the survey note, module-slice note, sample starter plus direct embedded sample replay, survey gate, module gate, diff gate, loader scaffold, shared loader binding, loader-gap note, and shared build entrypoint in one reviewable packet"
    },
    {
      "id": "shared-loader-controls-blocker",
      "kind": "runtime_control_surface",
      "expected": "the survey packet keeps broader shared runtime-loader command-name, argv-policy, and environment-derived activation handling blocked so the kretprobe pilot remains pre-execution"
    },
    {
      "id": "roadmap-gap-vs-pilot-module",
      "kind": "roadmap_gap",
      "expected": "the survey packet states that selftest hooks are landed, starter lifecycle transitions are reviewable, and the remaining `first loadable Zigux runtime modules` plus `runtime module lifecycle parity` roadmap steps stay blocked until the shared runtime-loader controls can support a real registration path"
    }
  ],
  "delivery_evidence_catalog": [
    {
      "id": "runtime-kretprobe-survey-note",
      "kind": "documentation",
      "path": "Documentation/zigux/phase9-runtime-kretprobe-survey.md",
      "role": "records the roadmap anchor, shipped review packet, ownership summary, shared-build evidence, and the exact remaining roadmap gap without implying live register or unregister parity"
    },
    ¶»§q«^