{
  "lane_key": "P5-L10",
  "phase": "Phase 5",
  "surveyed_commit": "affdebd460c9c33ce939c7535cdb929352648e93",
  "anchor": "samples/kobject/kobject-example.c",
  "sample_path": "samples/zigux/kobject_example.zig",
  "validation_entrypoint": "zig build test --build-file zigux/tests/phase5_build.zig --summary all",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest and survey test still describe the exact registration, single register_runs ownership claim, Linux foo/baz/bar attribute order, shared 0664 attribute mode pattern, integer roundtrip, and shared baz/bar dispatch contract run through phase5_build.zig",
    "the replay contract keeps the static directory-name cue explicit, keeps emits_uevent false, and says dynamic kobjects stay out of scope for this Phase 5 sample",
    "the sample-backed survey note, shared docs-root guide, shared sample-root catalog, shared tests-root guide, and shared review checklist still name the manifest-backed survey, sample-backed survey note, and phase5_build.zig entrypoint as part of the shipped kobject review surface",
    "the manifest prompts keep the unnamed attribute group shape plus the pre-registration ownership boundary, the sample-owned runOwnershipReplay() helper, initialized-only exit summary, ownershipSummary lifecycle snapshot, and sample-owned post-exit init/register/show/store rejection boundaries explicit instead of implying sysfs registration",
    "sample-behavior changes update the manifest-backed registration, attribute-order, teardown-summary, replay-readiness, lifecycle replay, and lifecycle contract instead of leaving reviewers to infer it from code alone",
    "ownership checks keep the initialized-but-not-registered stage explicit by requiring zero active attributes and no show/store access until registerAttributes claims ownership, and ownershipSummary must expose replay readiness plus the cold, initialized, registered, and exited stage transitions directly through the sample-owned runOwnershipReplay() helper",
    "docs and tests keep the sample-owned post-exit init/register/show/store boundary explicit while still keeping sysfs creation, kernel_kobj integration, uevents, and runtime registration out of scope for this Phase 5 sample, and keep the shared sample-root packet explicit that the landed slice is an approved Phase 5 ownership-and-lifetime idiom"
  ],
  "exact_checks": [
    {
      "id": "directory-name",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps the Linux directory name kobject_example and an unnamed attribute group"
    },
    {
      "id": "attribute-order",
      "kind": "anchor_shape",
      "expected": "the replay summary keeps the Linux attribute array order foo, baz, bar explicit"
    },
    {
      "id": "attribute-mode",
      "kind": "anchor_shape",
      "expected": "the replay summary keeps the Linux foo, baz, and bar attribute mode pattern explicit as 0664 for all three attributes"
    },
    {
      "id": "registration-step",
      "kind": "ownership_lifetime",
      "expected": "runAnchorReplay requires init first, claims attribute ownership through exactly one register_runs increment, registers exactly three attributes, and leaves the sample registered with attributes accessible"
    },
    {
      "id": "static-name-no-uevent-boundary",
      "kind": "anchor_shape",
      "expected": "the replay summary keeps the static directory name explicit, records no uevent delivery, and leaves dynamic kobjects out of scope for this sample"
    },
    {
      "id": "pre-registration-boundary",
      "kind": "ownership_lifetime",
      "expected": "the initialized-but-not-registered stage keeps the active attribute count at zero and rejects show or store calls until registerAttributes claims ownership"
    },
    {
      "id": "replay-readiness-boundary",
      "kind": "ownership_lifetime",
      "expected": "ownershipSummary keeps runAnchorReplay reviewable by allowing it only in the initialized stage and rejecting replay before registration ownership exists or after the sample has registered or exited, while the sample-owned runOwnershipReplay() helper replays the full cold-to-exited lifecycle explicitly"
    },
    {
      "id": "ownership-summary",
      "kind": "ownership_lifetime",
      "expected": "ownershipSummary reports replay readiness plus the cold, initialized, registered, and exited stages directly through the sample-owned runOwnershipReplay() helper, keeps active attribute counts at 0, 0, 3, and 0, and makes register-or-exit availability reviewable without reading internal fields"
    },
    {
      "id": "initialized-exit-teardown",
      "kind": "ownership_lifetime",
      "expected": "exit from the initialized stage returns an abandoned-before-registration teardown summary, keeps registerRuns at zero, and still leaves the sample exited with no active attributes"
    },
    {
      "id": "foo-roundtrip",
      "kind": "attribute_roundtrip",
      "expected": "storing 42 into foo renders back as the string 42 followed by a newline"
    },
    {
      "id": "shared-b-dispatch",
      "kind": "attribute_roundtrip",
      "expected": "baz and bar share the same show and store path while still rendering 7 and -5 through their own attribute names"
    },
    {
      "id": "parse-failure",
      "kind": "input_validation",
      "expected": "non-integer writes return InvalidInteger and unknown attribute names stay explicit errors"
    },
    {
      "id": "exit-boundary",
      "kind": "ownership_lifetime",
      "expected": "registered exit returns a teardown summary, then the sample-owned post-exit replay clears the tracked values, removes the active attribute count, and rejects later init, registerAttributes, runAnchorReplay, showValue, storeValue, or exit calls"
    }
  ],
  "non_goals": [
    "sysfs file creation parity",
    "kernel_kobj integration",
    "uevent delivery",
    "loadable module registration"
  ]
}