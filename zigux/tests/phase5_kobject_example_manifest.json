{
  "lane_key": "P5-L22",
  "phase": "Phase 5",
  "surveyed_commit": "2b5f92f6cf683de6fb54a81fb5374c78e9e2d9f5",
  "anchor": "samples/kobject/kobject-example.c",
  "sample_path": "samples/zigux/kobject_example.zig",
  "validation_entrypoint": "zig build test --build-file zigux/tests/phase5_build.zig --summary all",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest and survey test still describe the exact registration, Linux foo/baz/bar attribute order, shared 0664 attribute mode pattern, integer roundtrip, and shared baz/bar dispatch contract run through phase5_build.zig",
    "the sample-backed survey note and shared review checklist still name the manifest-backed survey, sample-backed survey note, and phase5_build.zig entrypoint as part of the shipped kobject review surface",
    "the manifest prompts keep the unnamed attribute group shape plus the pre-registration and post-exit init/register/show/store rejection boundaries explicit instead of implying sysfs registration",
    "sample-behavior changes update the manifest-backed registration, attribute-order, and lifecycle contract instead of leaving reviewers to infer it from code alone",
    "ownership checks keep the initialized-but-not-registered stage explicit by requiring zero active attributes and no show/store access until registerAttributes claims ownership",
    "docs and tests keep the post-exit init/register/show/store boundary explicit while still keeping sysfs creation, kernel_kobj integration, uevents, and module registration out of scope for this Phase 5 sample"
  ],
  "exact_checks": [
    {
      "id": "directory-name",
      "kind": "anchor_shape",
      "expected": "the in-memory sample keeps the Linux directory name kobject_example and an unnamed attribute group"
    },
    {
      "id": "attribute-order",
      "kind": "anchor_shape",
      "expected": "the replay summary keeps the Linux attribute array order foo, baz, bar explicit"
    },
    {
      "id": "attribute-mode",
      "kind": "anchor_shape",
      "expected": "the replay summary keeps the Linux foo, baz, and bar attribute mode pattern explicit as 0664 for all three attributes"
    },
    {
      "id": "registration-step",
      "kind": "ownership_lifetime",
      "expected": "runAnchorReplay requires init first, registers exactly three attributes, and leaves the sample in the registered stage"
    },
    {
      "id": "pre-registration-boundary",
      "kind": "ownership_lifetime",
      "expected": "the initialized-but-not-registered stage keeps the active attribute count at zero and rejects show or store calls until registerAttributes claims ownership"
    },
    {
      "id": "foo-roundtrip",
      "kind": "attribute_roundtrip",
      "expected": "storing 42 into foo renders back as the string 42 followed by a newline"
    },
    {
      "id": "shared-b-dispatch",
      "kind": "attribute_roundtrip",
      "expected": "baz and bar share the same show and store path while still rendering 7 and -5 through their own attribute names"
    },
    {
      "id": "parse-failure",
      "kind": "input_validation",
      "expected": "non-integer writes return InvalidInteger and unknown attribute names stay explicit errors"
    },
    {
      "id": "exit-boundary",
      "kind": "ownership_lifetime",
      "expected": "exit clears the tracked values, removes the active attribute count, and rejects later show or store calls"
    }
  ],
  "non_goals": [
    "sysfs file creation parity",
    "kernel_kobj integration",
    "uevent delivery",
    "loadable module registration"
  ]
}
