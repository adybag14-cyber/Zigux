{
  "lane_key": "P13-L12",
  "phase": "Phase 13",
  "surveyed_commit": "64617ec0339f3f52accf5614bc918a940a503f7a",
  "anchor": "security/landlock/ruleset.c",
  "roadmap_destinations": [
    "security/landlock/ruleset.zig",
    "zigux/tests/",
    "Documentation/zigux/"
  ],
  "survey_summary": {
    "ruleset_c_lines": 719,
    "landlock_security_file_count": 31,
    "preexisting_phase13_build_present": true,
    "preexisting_phase13_make_target_present": true,
    "preexisting_ruleset_zig_present": true,
    "preexisting_phase13_landlock_test_present": true,
    "preexisting_phase13_landlock_slice_note_present": true,
    "preexisting_phase13_landlock_survey_note_present": true
  },
  "gaps": [
    {
      "id": "phase13-build-gate",
      "status": "starter_landed",
      "kind": "validation",
      "zigux_destination": "zigux/tests/phase13_build.zig",
      "why_now": "The shared Phase 13 build now compiles the first landlock ruleset helper lab beside libfs and devres so this security-helper lane is reviewable without pretending it belongs in the live kernel build yet."
    },
    {
      "id": "phase13-make-target",
      "status": "starter_landed",
      "kind": "validation",
      "zigux_destination": "zigux/Makefile",
      "why_now": "The existing Phase 13 make target keeps the bounded ruleset helper lab runnable through the same repeatable tranche gate as the other shared-helper lanes."
    },
    {
      "id": "phase13-landlock-ruleset-starter",
      "status": "starter_landed",
      "kind": "security_helper_pilot",
      "zigux_destination": "security/landlock/ruleset.zig",
      "why_now": "The first honest `ruleset.zig` foothold stays in-memory around `landlock_create_ruleset()`, `landlock_union_access_masks()`, `landlock_init_layer_masks()`, and `landlock_unmask_layers()` so Zigux can model layer-stack access accounting before any rb-tree mutation, object references, or live LSM hooks are attempted."
    },
    {
      "id": "phase13-landlock-ruleset-test-gate",
      "status": "starter_landed",
      "kind": "validation",
      "zigux_destination": "zigux/tests/phase13_landlock_ruleset.zig",
      "why_now": "A dedicated Phase 13 test gate now checks both the helper behavior and the survey manifest so future runs can see the current ruleset helper lab and the still-missing deeper kernel state in one place."
    },
    {
      "id": "phase13-landlock-ruleset-slice-note",
      "status": "starter_landed",
      "kind": "documentation",
      "zigux_destination": "Documentation/zigux/phase13-landlock-ruleset-slice.md",
      "why_now": "The slice note records the exact helper-first boundary of the new ruleset lab so the lane does not overstate access-mask bookkeeping as full Landlock policy enforcement."
    },
    {
      "id": "phase13-landlock-ruleset-survey-note",
      "status": "starter_landed",
      "kind": "documentation",
      "zigux_destination": "Documentation/zigux/phase13-landlock-ruleset-survey.md",
      "why_now": "The survey note compares the 719-line `ruleset.c` anchor against the current helper lab and keeps the remaining gap around live tree state and hierarchy lifetime explicit."
    },
    {
      "id": "phase13-landlock-ruleset-ownership-note",
      "status": "starter_landed",
      "kind": "documentation",
      "zigux_destination": "Documentation/zigux/phase13-landlock-ruleset-ownership.md",
      "why_now": "A dedicated ownership note now fences `security/landlock/ruleset.zig` away from `security/landlock/syscalls.zig`, keeps `rb_replace_node()` and object ownership inside the blocked live-tree state, and makes the manifest, survey, slice, and test gate move together when the helper-owned surface changes."
    },
    {
      "id": "phase13-landlock-rule-layer-merge-followup",
      "status": "starter_landed",
      "kind": "security_helper_pilot",
      "zigux_destination": "security/landlock/ruleset.zig",
      "why_now": "The ruleset helper now includes one bounded in-memory planner around `insert_rule()` access extension versus merged-layer intersection so Zigux can exercise the matching-rule branch logic before adding rb-tree storage, locking, or object ownership."
    },
    {
      "id": "phase13-landlock-tree-search-followup",
      "status": "starter_landed",
      "kind": "security_helper_pilot",
      "zigux_destination": "security/landlock/ruleset.zig",
      "why_now": "The ruleset helper now adds a small planner for `insert_rule()` tree-search outcomes around `get_root()`, `walker_node`, and the no-match insertion-count branch, keeping the root choice and descent decisions explicit without mutating rb-trees."
    },
    {
      "id": "phase13-landlock-tree-link-followup",
      "status": "starter_landed",
      "kind": "security_helper_pilot",
      "zigux_destination": "security/landlock/ruleset.zig",
      "why_now": "The ruleset helper now includes one tiny in-memory planner for the no-match insertion branch around `rb_link_node()` and `rb_insert_color()` after the tree-search outcome is known, keeping the parent or root link mode explicit without mutating live rb-trees."
    },
    {
      "id": "phase13-landlock-live-tree-state-blocker",
      "status": "blocked_on_live_lsm_state",
      "kind": "security_helper_pilot",
      "zigux_destination": "security/landlock/ruleset.zig",
      "why_now": "The next remaining ruleset gap crosses into `rb_replace_node()`, object ownership, hierarchy lifetime, and other live state that would overstate this helper lab if it were modeled as a pure in-memory followup."
    }
  ]
}