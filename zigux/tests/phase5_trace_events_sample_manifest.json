{
  "lane_key": "P5-L16",
  "phase": "Phase 5",
  "surveyed_commit": "368dcb11d347e77c13bef6607bd99b313573e389",
  "anchor": "samples/trace_events/trace-events-sample.c",
  "sample_path": "samples/zigux/trace_events_sample.zig",
  "validation_entrypoint": "zig test zigux/tests/phase5_trace_events_sample_survey.zig",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest keeps surveyed_commit pinned to the exact inspected master head and the sample-backed survey note carries the same PHASE5_SURVEYED_COMMIT marker instead of a floating branch label so reviewability stays tied to one bounded repo state",
    "the sample-backed survey note, samples/zigux/README.md, Documentation/zigux/README.md, and Documentation/zigux/review-checklist.md still describe the same approved payload-and-callback idiom, keep the bounded trace-events replay contract reviewable and repeatable, and keep this Phase 5 sample separate from the Phase 9 runtime pilot",
    "the manifest and survey test still describe the exact selected-string-slot, payload-length, main-iteration, callback-iteration, vararg-payload, lifecycle-summary, message, relative-location, callback-path, and teardown contract through `zig test samples/zigux/trace_events_sample.zig`, `zig test zigux/tests/phase5_trace_events_sample.zig`, and `zig test zigux/tests/phase5_trace_events_sample_survey.zig` instead of the older missing shared `phase5_build.zig` route",
    "the public lifecycle summary still exposes stage plus init or replay or exit counts, registration depth, and total event calls so ownership progression stays reviewable without private field access",
    "the in-memory replay and dedicated `runStringFormattingCycleReplay()` summary still keep the array payload, the full modulo-selected string cycle, selected-string slot cues, and iter-format messages reviewable instead of hiding them behind runtime thread state",
    "function-callback replay stays a single live register-then-unregister idiom, rejects replayFunctionIteration before registration with FunctionCallbackNotRegistered, still rejects unregisterFunctionCallback underflow before registration, and still rejects exit with OutstandingRegistration while one callback remains armed rather than implying kthread creation or tracepoint enablement parity",
    "after `exit()` the sample still rejects replayMainIteration, registerFunctionCallback, replayFunctionIteration, and unregisterFunctionCallback so the teardown boundary stays fully explicit",
    "sample-behavior changes update the manifest-backed replay contract instead of leaving reviewers to infer the new boundary from code alone",
    "docs and tests still keep CREATE_TRACE_POINTS, tracepoint macros, kernel scheduling, and module registration wiring out of scope for this Phase 5 sample"
  ],
  "exact_checks": [
    {
      "id": "descriptor-anchor",
      "kind": "anchor_shape",
      "expected": "the in-memory sample names samples/trace_events/trace-events-sample.c and stays in the non-runtime reference-sample lane"
    },
    {
      "id": "message-and-string-shape",
      "kind": "bounded_payload",
      "expected": "runAnchorReplay formats iter=7, selects Gandalf from the Linux random_strings table for len 2, and exposes selected-string slot 2 in the public replay summary"
    },
    {
      "id": "modulo-string-cycle",
      "kind": "bounded_payload",
      "expected": "the dedicated `runStringFormattingCycleReplay()` summary replays counts 0 through 4 through five public cases so the full modulo-selected string cycle stays explicit: Mother Goose, Snoopy, Gandalf, Frodo, and One ring to rule them all"
    },
    {
      "id": "iteration-cues",
      "kind": "bounded_payload",
      "expected": "the replay summary exposes main iteration 7 and function-callback iteration 9 so the two Linux cnt-driven paths stay reviewable without reading private sample state"
    },
    {
      "id": "array-and-sentinel-shape",
      "kind": "bounded_payload",
      "expected": "the replay exposes payload length 2 and keeps a 1,2 payload prefix with a zero sentinel in the next slot so the Linux array idiom remains reviewable in memory"
    },
    {
      "id": "bitmask-and-rel-loc",
      "kind": "bounded_payload",
      "expected": "the replay records the 0xdeadbeef bitmask word and marks the relative-location payload path as checked in the replay summary"
    },
    {
      "id": "vararg-payload-path",
      "kind": "bounded_payload",
      "expected": "the replay marks the vararg payload path as checked so the fmt plus va_list trace_foo_bar idiom is explicit in the public replay summary"
    },
    {
      "id": "event-family-counts",
      "kind": "bounded_summary",
      "expected": "the replay records six main-thread event calls and two function-callback event calls for a total of eight bounded tracepoint-family calls after the public main-iteration and callback-iteration cues have been captured"
    },
    {
      "id": "lifecycle-summary-counts",
      "kind": "ownership_lifetime",
      "expected": "the public lifecycle summary reports replay_complete with init or replay or exit counts 1,1,0 plus zero registration depth and eight total event calls after runAnchorReplay, then reports exited with counts 1,1,1 after exit"
    },
    {
      "id": "checked-focus-order",
      "kind": "review_surface",
      "expected": "the replay summary keeps the exact checked_focus order payload_shape,string_selection,formatted_message,conditional_event_families,function_callback_registration,ownership_and_lifetime so reviewers can see the approved trace-events reading contract directly"
    },
    {
      "id": "callback-registration-balance",
      "kind": "ownership_lifetime",
      "expected": "the function-callback replay requires registration first, marks that callback path as checked, and restores the registration balance to zero before the sample completes"
    },
    {
      "id": "pre-registration-callback-rejection",
      "kind": "ownership_lifetime",
      "expected": "before a callback is registered, the in-memory sample rejects replayFunctionIteration before registration with FunctionCallbackNotRegistered so the callback lane stays reviewable without implying tracepoint enablement parity"
    },
    {
      "id": "single-registration-boundary",
      "kind": "ownership_lifetime",
      "expected": "the in-memory callback lane rejects a second registerFunctionCallback call while already registered so the Phase 5 sample keeps one live callback registration before balance returns to zero"
    },
    {
      "id": "registration-underflow-and-armed-exit",
      "kind": "ownership_lifetime",
      "expected": "before balance returns to zero, the same ownership lane rejects unregisterFunctionCallback underflow before registration and rejects exit with OutstandingRegistration while one callback remains armed"
    },
    {
      "id": "post-exit-rejection",
      "kind": "ownership_lifetime",
      "expected": "after exit the sample rejects later replayMainIteration, registerFunctionCallback, replayFunctionIteration, and unregisterFunctionCallback calls"
    }
  ],
  "non_goals": [
    "CREATE_TRACE_POINTS parity",
    "tracepoint macro parity from trace-events-sample.h",
    "kernel thread scheduling or timeout parity",
    "module registration or unregister wiring parity"
  ]
}