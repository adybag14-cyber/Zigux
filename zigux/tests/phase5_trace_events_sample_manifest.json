{
  "lane_key": "P5-L13",
  "phase": "Phase 5",
  "surveyed_commit": "3ea8f93f1e475fcfa89a5e46940cc82be01f2971",
  "anchor": "samples/trace_events/trace-events-sample.c",
  "sample_path": "samples/zigux/trace_events_sample.zig",
  "validation_entrypoint": "zig build test --build-file zigux/tests/phase5_build.zig --summary all",
  "review_prompts": [
    "descriptor keeps the Linux anchor path explicit and leaves requires_runtime_substrate false while provides_selfcheck stays true",
    "the manifest and survey test still describe the exact vararg-payload, message, relative-location, callback-path, and teardown contract run through phase5_build.zig",
    "the contributor packet still keeps formattedMessage() explicit as the approved public formatting surface for the selected-string plus iter=%d replay instead of implying direct reads of private message storage",
    "the contributor packet still names runPayloadBoundaryReplay() as the approved public count-4 payload-boundary helper instead of implying private field inspection for the 1,2,3,4 prefix, zero sentinel, iter=%d replay, and selected-string branch",
    "the contributor packet still names runConditionalBoundaryReplay() as the approved public conditional-family helper for the full 0..5 selected-string and iter=%d cycle, namely Mother Goose, Snoopy, Gandalf, Frodo, One ring to rule them all, and the count-5 wraparound back to Mother Goose, plus the 0xdeadbeef bitmask cue and main-thread family counts instead of implying private sample-state reads",
    "the in-memory replay still keeps the array payload, selected string, and iter-format message reviewable instead of hiding them behind runtime thread state",
    "runCallbackBoundaryReplay() keeps function-callback replay a balanced register-then-unregister idiom rather than implying kthread creation or tracepoint enablement parity",
    "the sample-owned prompts keep the exact checked_focus order, the public ownershipSummary() lifecycle snapshot helper plus runOwnershipReplay() as the approved public lifecycle replay helper, the registration depth rising to 1 then returning to 0 across the balanced callback replay, unregisterFunctionCallback() underflow plus OutstandingRegistration rejection, and post-exit replay rejection explicit instead of leaving those callback-boundary and ownership-lifetime cues implied",
    "sample-behavior changes update the manifest-backed replay contract instead of leaving reviewers to infer the new boundary from code alone",
    "docs and tests still keep CREATE_TRACE_POINTS, tracepoint macros, kernel scheduling, and module registration wiring out of scope for this Phase 5 sample"
  ],
  "exact_checks": [
    {
      "id": "descriptor-anchor",
      "kind": "anchor_shape",
      "expected": "the in-memory sample names samples/trace_events/trace-events-sample.c and stays in the non-runtime reference-sample lane"
    },
    {
      "id": "message-and-string-shape",
      "kind": "bounded_payload",
      "expected": "runAnchorReplay formats iter=7 and selects Gandalf from the Linux random_strings table for len 2"
    },
    {
      "id": "array-and-sentinel-shape",
      "kind": "bounded_payload",
      "expected": "the replay keeps a 1,2 payload prefix with a zero sentinel in the next slot so the Linux array idiom remains reviewable in memory"
    },
    {
      "id": "public-payload-boundary-helper",
      "kind": "bounded_payload",
      "expected": "runPayloadBoundaryReplay keeps the count-4 iter=4 payload boundary public by returning the 1,2,3,4 prefix, zero sentinel, selected string One ring to rule them all, and initialized-stage boundary without private sample-state reads"
    },
    {
      "id": "public-conditional-boundary-helper",
      "kind": "bounded_summary",
      "expected": "runConditionalBoundaryReplay keeps the full public 0..5 selected-string and iter=%d cycle reviewable by returning Mother Goose, Snoopy, Gandalf, Frodo, One ring to rule them all, and the count-5 wraparound back to Mother Goose, plus the 0xdeadbeef bitmask word and six main-thread event calls without private sample-state reads"
    },
    {
      "id": "bitmask-and-rel-loc",
      "kind": "bounded_payload",
      "expected": "the replay records the 0xdeadbeef bitmask word and marks the relative-location payload path as checked in the replay summary"
    },
    {
      "id": "vararg-payload-path",
      "kind": "bounded_payload",
      "expected": "the replay marks the vararg payload path as checked so the fmt plus va_list trace_foo_bar idiom is explicit in the public replay summary"
    },
    {
      "id": "event-family-counts",
      "kind": "bounded_summary",
      "expected": "the replay records six main-thread event calls and two function-callback event calls for a total of eight bounded tracepoint-family calls"
    },
    {
      "id": "callback-registration-balance",
      "kind": "ownership_lifetime",
      "expected": "the public runCallbackBoundaryReplay() helper records the exact checked_focus order, the callback path, the count-specific callback replay, the registration depth rising to 1 after register and returning to 0 after unregister, unregisterFunctionCallback() underflow plus OutstandingRegistration rejection, and the restored registration balance at zero without private sample-state reads before the sample completes"
    },
    {
      "id": "post-exit-rejection",
      "kind": "ownership_lifetime",
      "expected": "ownershipSummary() keeps the cold, initialized, replay_complete, and exited lifecycle snapshots public with one init, replay, and exit run, Gandalf plus iter=7 retained in the replayed snapshot, restored registration balance, and the after exit state that rejects later replay or callback registration"
    }
  ],
  "non_goals": [
    "CREATE_TRACE_POINTS parity",
    "tracepoint macro parity from trace-events-sample.h",
    "kernel thread scheduling or timeout parity",
    "module registration or unregister wiring parity"
  ]
}